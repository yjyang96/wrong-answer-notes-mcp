# Story 4.2: Code Patch Generation

## Status
Draft

## Story
**As a** AI engineer,
**I want** 검색된 커밋을 참고하여 코드 패치를 생성하는 시스템,
**so that** 사용자가 직접 적용할 수 있는 구체적인 수정안을 제공할 수 있습니다.

## Acceptance Criteria
1. Diff 형태의 코드 패치가 생성됩니다.
2. 수정 전후 코드의 차이점이 명확히 표시됩니다.
3. 패치 적용 방법과 주의사항이 포함됩니다.
4. 생성된 패치의 문법적 정확성이 검증됩니다.
5. 패치 생성 과정의 재현성이 보장됩니다.

## Tasks / Subtasks
- [ ] Task 1: Diff 형태 패치 생성 (AC: 1)
  - [ ] Git Diff 형식 구현
  - [ ] 코드 변경사항 추출
  - [ ] 패치 포맷팅
  - [ ] 패치 검증
- [ ] Task 2: 코드 차이점 표시 (AC: 2)
  - [ ] Before/After 코드 비교
  - [ ] 변경 라인 하이라이팅
  - [ ] 변경 유형 분류
  - [ ] 시각적 차이점 표시
- [ ] Task 3: 적용 방법 및 주의사항 (AC: 3)
  - [ ] 패치 적용 가이드 생성
  - [ ] 주의사항 및 경고 메시지
  - [ ] 의존성 체크
  - [ ] 롤백 방법 안내
- [ ] Task 4: 문법 정확성 검증 (AC: 4)
  - [ ] 코드 문법 검사
  - [ ] 타입 체크
  - [ ] 린트 규칙 검증
  - [ ] 컴파일 가능성 확인
- [ ] Task 5: 재현성 보장 시스템 (AC: 5)
  - [ ] 패치 생성 로깅
  - [ ] 결정 과정 기록
  - [ ] 재생성 메커니즘
  - [ ] 일관성 검증

## Dev Notes

### Previous Story Insights
[Source: docs/stories/4.1.llm-integration.md]
- Azure OpenAI GPT-4o API 통합이 구현됨
- 커밋 컨텍스트를 포함한 프롬프트 템플릿이 설계됨
- LLM 환각 방지 가드레일이 구현됨

### Technical Constraints
[Source: docs/prd.md#functional-requirements]
- **FR5**: 검색된 커밋과 Diff를 LLM에 주입하여 근거와 함께 코드 패치 제안을 생성할 수 있어야 합니다.
- **Patch Format**: Git Diff 표준 형식
- **Accuracy**: 문법적 정확성 검증
- **Reproducibility**: 패치 생성 과정 재현성

### Code Patch Generation Architecture
[Source: docs/prd.md#technical-assumptions]
```
Code Patch Generation:
├── Input Processing:
│   ├── Commit Context
│   ├── User Query
│   └── Current Code
├── Patch Generation:
│   ├── LLM Processing
│   ├── Diff Creation
│   └── Format Validation
├── Quality Assurance:
│   ├── Syntax Validation
│   ├── Type Checking
│   └── Lint Verification
└── Output Generation:
    ├── Patch Formatting
    ├── Application Guide
    └── Warning Messages
```

### File Locations
- 패치 생성 서비스: `llm-service/src/patch-generation/`
- 검증 시스템: `llm-service/src/validation/`
- 포맷팅: `llm-service/src/formatting/`
- 가이드 생성: `llm-service/src/guides/`

### Patch Data Structure
[Source: docs/prd.md#functional-requirements]
```typescript
interface CodePatch {
  id: string;
  format: 'git-diff' | 'unified-diff';
  content: string;
  metadata: {
    filePath: string;
    lineNumbers: {
      start: number;
      end: number;
    };
    changeType: 'addition' | 'deletion' | 'modification';
  };
  validation: {
    syntaxValid: boolean;
    typeValid: boolean;
    lintPassed: boolean;
  };
  applicationGuide: {
    steps: string[];
    warnings: string[];
    rollbackInstructions: string[];
  };
}
```

## Testing

### Testing Standards
- **Unit Testing**: 각 패치 생성 컴포넌트별 테스트
- **Integration Testing**: LLM 기반 패치 생성 통합 테스트
- **Validation Testing**: 패치 검증 시스템 테스트
- **Format Testing**: Diff 형식 정확성 테스트

### Specific Testing Requirements for This Story
- Diff 형태의 코드 패치가 올바르게 생성되는지 확인
- 수정 전후 코드의 차이점이 명확히 표시되는지 확인
- 패치 적용 방법과 주의사항이 올바르게 포함되는지 확인
- 생성된 패치의 문법적 정확성이 올바르게 검증되는지 확인
- 패치 생성 과정의 재현성이 보장되는지 확인

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|---------|
| 2025-01-17 | v1.0 | 초기 Story 작성 | Bob (Scrum Master) |

## Dev Agent Record
*This section will be populated by the development agent during implementation*

### Agent Model Used
*To be filled by dev agent*

### Debug Log References
*To be filled by dev agent*

### Completion Notes List
*To be filled by dev agent*

### File List
*To be filled by dev agent*

## QA Results
*Results from QA Agent review will be populated here*

